<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="match_parent">

    <ImageView
        android:id="@+id/image_upload_imageview"
        android:layout_width="@dimen/margin_80_dp"
        android:layout_height="@dimen/margin_80_dp"
        android:layout_alignParentLeft="true"
        android:layout_centerVertical="true"
        android:layout_margin="@dimen/margin_8_dp"
        android:contentDescription="@null"
        android:scaleType="center"
    />


    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/margin_8_dp"
        android:orientation="vertical"
        android:layout_toRightOf="@id/image_upload_imageview"
    >
        <TextView
            android:id="@+id/image_upload_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/text_black"
            android:singleLine="true"
            android:textSize="@dimen/font_size_18"
        />

        <TextView
            android:id="@+id/image_upload_progress_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/margin_5_dp"
            android:textColor="@color/blue"
            android:textSize="@dimen/font_size_13"
        />
        <ProgressBar
            android:layout_marginTop="@dimen/margin_5_dp"
            android:id="@+id/image_upload_progress"
            style="@style/Base.Widget.AppCompat.ProgressBar.Horizontal"
            android:layout_width="match_parent"
            android:layout_height="@dimen/margin_8_dp"
            android:layout_gravity="bottom"
            android:background="@color/transparent"
            android:layout_marginBottom="@dimen/margin_5_dp"
            android:max="100"
            android:progressDrawable="@drawable/electory_progressbar_drawable"
        />
    </LinearLayout>
</RelativeLayout>